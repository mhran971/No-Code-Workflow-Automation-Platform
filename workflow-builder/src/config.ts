/**
 * Hard-code your API settings here for local testing.
 * You can also override these in the UI; values are persisted in localStorage.
 */
// Use the Vite dev proxy (same origin) or the full Laravel URL.
export const DEFAULT_API_BASE_URL = '/api/v1';

/** Paste a JWT access token from POST /api/v1/login */
export const DEFAULT_ACCESS_TOKEN = '';
